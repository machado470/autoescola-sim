#!/bin/sh
set -euo pipefail

# Nenhuma migration automática deve rodar aqui.
# O container sobe limpo e pronto.

exec "$@"
#!/bin/sh
set -euo pipefail

# Nenhuma migration automática deve rodar aqui.
# O container sobe limpo e pronto.

exec "$@"
